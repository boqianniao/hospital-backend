# 启动指南

数据库、Redis、Elasticsearch 等中间件地址已经配置好。第三方云服务密钥不提交到 Git,支付宝本机凭证保存在已忽略的 `config/application-alipay-local.yml` 中。

- `hospital-backend/`(本项目):后端,端口 `8080`
- `../hospital/`:前端页面

> 先起后端,再打开前端。前端所有请求都打到 `http://localhost:8080`。

---

## 一、启动后端

需要 **JDK 21**。用 IntelliJ IDEA 打开 `hospital-backend` 目录,等依赖下载完,运行主类:

```
src/main/java/com/hospital/HospitalApplication.java
```

控制台出现 `Started HospitalApplication` 即成功。

命令行方式(在 `hospital-backend` 目录下):

```bash
mvn spring-boot:run
```

验证:浏览器打开 `http://localhost:8080/doc.html` 能看到接口文档,就说明后端起好了。

---

## 二、启动前端

前端是原生页面,**不用装依赖、不用打包**,起个静态服务器打开即可。

**方式一 · IDEA(推荐):** 用 IDEA 打开 `hospital` 目录 → 打开 `index.html` → 点右上角浏览器图标,访问:

```
http://localhost:63342/hospital/index.html
```

**方式二 · 命令行:**

```bash
cd ../hospital
python3 -m http.server 5500
# 浏览器打开 http://localhost:5500/index.html
```

打开首页能看到医院/医生等数据,就说明前后端已连通。

---

## 三、支付宝沙箱支付

支付宝沙箱应用私钥和支付宝公钥保存在本机 `config/application-alipay-local.yml` 中,该文件已被 Git 忽略。`application.yml` 已把 `alipay-local` 设为默认 Profile,本机直接启动就会自动加载；后端出现 `[ALIPAY] 初始化完成` 日志后即可测试。

命令行启动方式：

```bash
mvn spring-boot:run
```

使用 IntelliJ 时可以直接运行主类；如运行配置显式填写了其他 Active profiles,需要同时加入 `alipay-local`。

### 沙箱应用与商家信息

| 项目 | 内容 |
|---|---|
| APPID | `9021000168601239` |
| 商户 PID | `2088721111952876` |
| 商家账号 | `huxgtt0133@sandbox.com` |
| 商家登录密码 | `111111` |
| 支付网关 | `https://openapi-sandbox.dl.alipaydev.com/gateway.do` |

### 沙箱买家账号

支付时使用下面的买家账号登录支付宝沙箱收银台。不要使用商家账号付款。

| 项目 | 内容 |
|---|---|
| 买家账号 | `albuet4927@sandbox.com` |
| 登录密码 | `111111` |
| 支付密码 | `111111` |
| 买家 UID | `2088722112009608` |

### 支付测试步骤

1. 登录前端,进入医生详情并创建挂号或电话咨询订单。
2. 在待支付订单中点击“继续支付”,或者下单后直接进入支付页。
3. 选择支付宝并确认支付,页面会跳转到支付宝沙箱收银台。
4. 使用上面的买家账号和支付密码完成沙箱付款。
5. 支付宝返回本地系统后,到“我的挂号”或“我的咨询”确认订单状态已经变为“已支付”。

沙箱支付不会扣真实资金。当前本地配置没有公网 `notify-url`,主要通过支付宝浏览器回跳后由后端验签和查单确认支付结果；如果要验证异步通知,需要通过环境变量 `ALIPAY_NOTIFY_URL` 配置支付宝可以访问的公网 HTTPS 地址。

默认支付完成后跳转到 `http://localhost:5500` 对应的“我的挂号”或“我的咨询”页面。如果前端使用 IDEA 的 `63342` 地址启动,请在启动后端前设置 `FRONTEND_BASE_URL=http://localhost:63342/hospital`。
